# Compare potential duplicates without modeling versions

Canopy v1 compares differently hashed Source PDFs that appear to represent the same work, but it does not merge them or record edition relationships. The user may add the candidate as a separate Paper, keep the existing Paper, or cancel remaining additions; true version history and annotation migration remain deferred because they require a trustworthy Edition model.
